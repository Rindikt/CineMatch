# 🎬 CineMatch: Асинхронная экосистема для киноманов (Docker + Celery)

## 🎯 Техническая Архитектура и Ключевые Фишки

Этот проект представляет собой полнофункциональную платформу для агрегации и управления данными о кино. Архитектура построена на принципах микросервисов, асинхронности и распределенной обработки задач.

### 1. Распределенная обработка задач (Celery & Redis)
Проект демонстрирует навыки работы с очередями задач, что критично для современных высоконагруженных систем:
* **Background Workers**: Все тяжелые операции (интеграция с внешним TMDB API, обработка изображений) вынесены в фоновые процессы Celery. Это позволяет API отвечать пользователю мгновенно, не дожидаясь ответа от сторонних сервисов.
* **Scheduled Tasks (Celery Beat)**: Реализовано автоматическое обновление базы данных по расписанию (синхронизация актуальных рейтингов и популярных новинок).
* **Reliability**: В качестве брокера сообщений используется **Redis**, обеспечивающий надежную доставку задач между API и воркерами.

### 2. Асинхронный Стек и SQLAlchemy 2.0
* **Backend**: Написан на **FastAPI**, что позволяет максимально эффективно использовать ресурсы CPU за счет неблокирующего ввода-вывода.
* **ORM**: Использование **SQLAlchemy 2.0** в связке с драйвером **asyncpg**. Это гарантирует высокую производительность при работе с базой данных PostgreSQL.
* **Migrations**: Управление схемой данных реализовано через **Alembic**, что обеспечивает безопасное обновление структуры БД.

### 3. Оркестрация и Инфраструктура (Docker Compose)
Для обеспечения консистентности окружения проект полностью контейнеризирован. Конфигурация включает в себя:
* **YAML-якоря**: Оптимизированный `docker-compose.yml` без дублирования кода.
* **Healthchecks**: Умный запуск сервисов — API и воркеры ждут полной готовности PostgreSQL (`service_healthy`), прежде чем начать работу.
* **Security**: Полная изоляция чувствительных данных (пароли, API-ключи) через переменные окружения и `.env` файлы.

### 4. Тестирование и Надежность
* **Isolated Testing Environment**: Настроен отдельный контейнер для автоматического запуска тестов (**Pytest**).
* **Атомарность**: Использование транзакционных откатов в тестах, что позволяет проверять логику импорта данных, не оставляя "мусора" в базе.

---

## 🛠 Технологический стек
* **Language**: Python 3.11+
* **Frameworks**: FastAPI, Celery
* **Databases**: PostgreSQL (16-alpine), Redis (6.2-alpine)
* **Libraries**: SQLAlchemy 2.0 (Async), Alembic, Pydantic v2, Pytest.
* **DevOps**: Docker, Docker Compose.

---

## ⚙️ Быстрый старт

Проект подготовлен к быстрому развертыванию через Docker:

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/Rindikt/CineMatch.git](https://github.com/Rindikt/CineMatch.git)
    cd CineMatch
    ```

2.  **Настройте переменные окружения:**
    Скопируйте шаблон и подставьте свои ключи (например, `TMDB_API_KEY`):
    ```bash
    cp .env.example .env
    ```

3.  **Запустите все сервисы:**
    ```bash
    docker-compose up --build
    ```

После запуска API будет доступно по адресу `http://localhost:8000`, а фронтенд — `http://localhost:8081`.

---
